# 2022 Original Implementation

This directory preserves the original Machine Vision university project developed in 2022.

The original files should remain unchanged so that the historical implementation can be compared with the modernized application.

The production package under `src/` must not import code directly from this directory.